version: 2.1
jobs:
  build:
    machine: true
    steps:
      - checkout
      # build the test image
      - run: docker build -t georgegillams-test -f Dockerfile.backstopjstest .
      # run the tests
      #- run: docker run --rm georgegillams-test

      - run: docker cp $(docker ps -a | grep georgegillams-test | awk '{print $1}'):/usr/src/tmp/backstop_data ./
      - run: git diff

        # - store_artifacts:
        #    path
